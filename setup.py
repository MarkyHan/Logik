from setuptools import setup
  
setup(
    name='my_package',
    version='0.1',
    description='A sample Python package',
    author='Markéta Hanušková',
    packages=['my_package'],
    install_requires=[
        ,
    ],
)
